const types = state => state.types;
const type = state => state.type;
const special_points = state => state.special_points;
const filtered_types = state => state.filtered_types;

export default {
    types,
    type,
    special_points,
    filtered_types
};
